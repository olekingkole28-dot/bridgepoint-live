name: Deploy BridgePoint Intelligence V5000 Rebuild
# combined surface republish: distribution-network-v5202

on:
  push:
    branches: [main]
    paths:
      - 'rebuild-v5000/**'
      - 'release-v2300/world-v2300-config.js'
      - 'release-v2300/world-v2300-map.js'
      - 'release-v2300/world-v2300-worker.js'
      - 'release-v2300/world-v2300.css'
      - 'release-v2300/world-v2300-inspector.js'
      - 'release-v2300/world-v2300-bootstrap.js'
      - 'release-v2300/world-v2300-weather.js'
      - 'release-v2300/world-v2300-present-weather.js'
      - '.github/workflows/deploy-intelligence-rebuild-v5000.yml'
      - 'app/horizon/**'
      - 'app/horizon-playable/**'
      - 'discover/**'
      - 'newsroom/**'
      - 'claims-terminal/**'
      - 'terminal/**'
      - 'integrity-index/**'
      - 'settlement-lab/**'
      - 'distribution/**'
      - 'public-audit/**'
      - 'portfolio-evidence/**'
      - 'data-discrepancy/**'
      - 'articles/**'
      - 'book/**'
      - 'pricing/**'
      - 'articles/bridgepoint-facts.json'
      - 'current-map-entry.js'
      - 'acquisition_tracker.js'
      - 'founder-access.js'
      - 'founder-access.css'
      - 'founder-access/**'
      - 'for-roofers/**'
      - 'for-solar/**'
      - 'for-restoration/**'
      - 'for-public-adjusters/**'
      - 'for-property-managers/**'
      - 'sample/**'
      - 'owner/acquisition/**'
      - 'owner/command/**'
      - 'downloads/backup/**'
      - 'enterprise/**'
      - 'investors/**'
      - 'press/**'
      - 'answers/**'
      - 'founder/**'
      - 'language-selector-v5200.css'
      - 'llms-full.txt'
      - 'llms.txt'
      - 'sitemap.xml'
      - 'robots.txt'
      - 'language-selector-v5200.js'
      - 'search-sitemap.xml'
      - 'bridgepoint-knowledge.jsonld'
      - 'ai-index.json'
      - 'discovery-facts.json'
      - 'search/**'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: bridgepoint-v5000-production-authority
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Build clean Intelligence-only artifact
        shell: bash
        run: |
          set -euo pipefail
          rm -rf site
          mkdir -p site
          git fetch origin live-artifact:refs/remotes/origin/live-artifact --depth=1
          git archive refs/remotes/origin/live-artifact | tar -xf - -C site
          mkdir -p site/vendor

          # Keep only the current Intelligence workspace plus the current Horizon lobby and playable runtime.
          # Other historical /app/... URLs fall through the V5000 404 back to the live map.
          rm -rf site/app site/discover site/search site/newsroom site/claims-terminal site/terminal site/integrity-index site/settlement-lab site/distribution site/public-audit site/portfolio-evidence site/data-discrepancy site/articles/auto site/founder-access site/for-roofers site/for-solar site/for-restoration site/for-public-adjusters site/for-property-managers site/sample site/founder site/answers site/press site/investors site/enterprise site/owner/acquisition site/owner/command
          mkdir -p site/app site/articles
          git archive refs/remotes/origin/live-artifact app/vendor | tar -xf - -C site
          cp -a discover site/discover
          if [ -d search ]; then cp -a search site/search; fi
          if [ ! -d newsroom ]; then python scripts/publish_growth_newsroom_v5100.py; fi
          cp -a newsroom site/newsroom
          cp -a claims-terminal site/claims-terminal
          cp -a terminal site/terminal
          cp -a integrity-index site/integrity-index
          cp -a settlement-lab site/settlement-lab
          cp -a distribution site/distribution
          cp -a public-audit site/public-audit
          cp -a portfolio-evidence site/portfolio-evidence
          cp -a data-discrepancy site/data-discrepancy
          (cd distribution && zip -qr ../site/distribution/bii-browser-extension.zip bii-browser-extension)
          (cd distribution && zip -qr ../site/distribution/google-sheets.zip google-sheets)
          (cd distribution/qgis-plugin && zip -qr ../../site/distribution/qgis-plugin.zip bridgepoint_intelligence)
          (cd distribution && zip -qr ../site/distribution/mcp-server.zip mcp)
          cp -a articles/auto site/articles/auto
          find articles -maxdepth 1 -type f -exec cp -f {} site/articles/ \;
          cp articles/bridgepoint-facts.json site/articles/bridgepoint-facts.json
          cp -a app/horizon site/app/horizon
          cp -a app/horizon-playable site/app/horizon-playable
          cp current-map-entry.js site/current-map-entry.js
          cp acquisition_tracker.js site/acquisition_tracker.js
          for f in discovery-facts.json ai-index.json bridgepoint-knowledge.jsonld search-sitemap.xml language-selector-v5200.js language-selector-v5200.css; do
            if [ -f "$f" ]; then cp "$f" "site/$f"; fi
          done
          cp founder-access.js site/founder-access.js
          cp founder-access.css site/founder-access.css
          cp -a founder-access site/founder-access
          cp -a for-roofers site/for-roofers
          cp -a for-solar site/for-solar
          cp -a for-restoration site/for-restoration
          cp -a for-public-adjusters site/for-public-adjusters
          cp -a for-property-managers site/for-property-managers
          cp -a sample site/sample
          cp -a founder site/founder
          cp -a answers site/answers
          cp -a press site/press
          if [ -d book ]; then cp -a book site/book; fi
          if [ -d pricing ]; then cp -a pricing site/pricing; fi
          if [ -d investors ]; then cp -a investors site/investors; fi
          if [ -d enterprise ]; then cp -a enterprise site/enterprise; fi
          mkdir -p site/owner
          cp -a owner/acquisition site/owner/acquisition
          cp -a owner/command site/owner/command
          mkdir -p site/downloads/backup
          cp downloads/backup/bridgepoint_full_export_windows.ps1 site/downloads/backup/bridgepoint_full_export_windows.ps1
          cp downloads/backup/bridgepoint_state_export_windows.ps1 site/downloads/backup/bridgepoint_state_export_windows.ps1
          cp downloads/backup/BridgePoint_State_Export_Wizard.bat site/downloads/backup/BridgePoint_State_Export_Wizard.bat
          cp rebuild-v5000/index.html site/index.html
          cp rebuild-v5000/app-index.html site/app/index.html
          cp rebuild-v5000/landing.css site/landing.css
          cp rebuild-v5000/landing.js site/landing.js
          cp rebuild-v5000/styles.css site/styles.css
          cp rebuild-v5000/app.js site/app.js
          cp rebuild-v5000/owner-everything.js site/owner-everything.js
          cp rebuild-v5000/owner-everything.css site/owner-everything.css
          cp rebuild-v5000/manifest.webmanifest site/manifest.webmanifest
          cp rebuild-v5000/favicon.ppm site/favicon.ppm
          cp rebuild-v5000/sw.js site/sw.js
          cp rebuild-v5000/404.html site/404.html
          cp rebuild-v5000/bridgepoint-mark.svg site/bridgepoint-mark.svg
          cp rebuild-v5000/bridgepoint-favicon-48.png site/bridgepoint-favicon-48.png
          rm -rf site/terms site/privacy
          cp -a rebuild-v5000/terms site/terms
          cp -a rebuild-v5000/privacy site/privacy
          cp robots.txt sitemap.xml llms.txt llms-full.txt site/

          # Funnel legacy public content to the current map and attach the language selector.
          python - <<'PY'
          from pathlib import Path
          funnel='<script defer src="/current-map-entry.js?v=5100"></script>'
          lang_js='<script defer src="/language-selector-v5200.js"></script>'
          lang_css='<link rel="stylesheet" href="/language-selector-v5200.css">'
          for p in Path('site').rglob('*.html'):
              rel=p.relative_to('site').as_posix()
              if rel.startswith(('owner/','admin/')):
                  continue
              text=p.read_text(encoding='utf-8',errors='ignore')
              language_ok=not rel.startswith(('app/horizon-playable/','distribution/gis-widget/'))
              if language_ok and lang_css not in text and '</head>' in text:
                  text=text.replace('</head>',lang_css+'</head>',1)
              if language_ok and lang_js not in text:
                  text=text.replace('</body>',lang_js+'</body>',1) if '</body>' in text else text+lang_js
              current_surface=rel.startswith(('terminal/','integrity-index/','settlement-lab/','distribution/','public-audit/','portfolio-evidence/','data-discrepancy/','claims-terminal/'))
              if rel not in {'index.html','app/index.html'} and not rel.startswith('app/') and not current_surface and funnel not in text:
                  text=text.replace('</body>',funnel+'</body>',1) if '</body>' in text else text+funnel
              p.write_text(text,encoding='utf-8')
          PY

          # /app is the full workspace; root is the public BridgePoint product surface.

          # Carry forward only the proven map engine pieces used by the new shell.
          cp release-v2300/world-v2300-config.js site/world-v2300-config.js
          cp release-v2300/world-v2300-map.js site/world-v2300-map.js
          cp release-v2300/world-v2300-worker.js site/world-v2300-worker.js
          cp release-v2300/world-v2300-weather.js site/world-v2300-weather.js
          cp release-v2300/world-v2300-present-weather.js site/world-v2300-present-weather.js
          cp release-v2300/world-v2300-config.js site/app/world-v2300-config.js
          cp release-v2300/world-v2300-map.js site/app/world-v2300-map.js
          cp release-v2300/world-v2300-worker.js site/app/world-v2300-worker.js
          cp release-v2300/world-v2300-weather.js site/app/world-v2300-weather.js
          cp release-v2300/world-v2300-present-weather.js site/app/world-v2300-present-weather.js
          cp release-v2300/world-v2300-inspector.js site/app/world-v2300-inspector.js
          cp release-v2300/world-v2300-bootstrap.js site/app/world-v2300-bootstrap.js
          cp release-v2300/world-v2300.css site/app/world-v2300.css

          curl --retry 4 --retry-all-errors --retry-delay 2 -fsSL https://cdn.jsdelivr.net/npm/maplibre-gl@5.6.2/dist/maplibre-gl.js -o site/vendor/maplibre-gl.js
          curl --retry 4 --retry-all-errors --retry-delay 2 -fsSL https://cdn.jsdelivr.net/npm/maplibre-gl@5.6.2/dist/maplibre-gl.css -o site/vendor/maplibre-gl.css
          curl --retry 4 --retry-all-errors --retry-delay 2 -fsSL https://cdn.jsdelivr.net/npm/pmtiles@4.5.0/dist/pmtiles.js -o site/vendor/pmtiles.js

          printf '%s\n' 'bridgepointintelligence.online' > site/CNAME
          touch site/.nojekyll site/app/.nojekyll

          node --check site/app.js
          node --check site/landing.js
          node --check site/acquisition_tracker.js
          grep -Fq 'us.i.posthog.com' site/acquisition_tracker.js
          grep -Fq 'HUMAN_INTERACTION' site/acquisition_tracker.js
          grep -Fq 'SIGNUP_SUCCESS' site/landing.js
          node --check site/world-v2300-config.js
          node --check site/world-v2300-map.js
          node --check site/world-v2300-worker.js
          node --check site/world-v2300-weather.js
          node --check site/world-v2300-present-weather.js
          node --check site/app/world-v2300-weather.js
          node --check site/app/world-v2300-present-weather.js
          node --check site/app/world-v2300-bootstrap.js
          node --check site/app/horizon/app.js
          node --check site/app/horizon/lobby-scene.js
          node --check site/app/horizon/model-preview.js
          node --check site/app/horizon-playable/horizon-v2.js

          grep -Fq 'BP_FRONTEND_V5000' site/index.html
          grep -Fq '__BP_APP_BUILD_VERSION__=5368' site/app.js
          grep -Fq '__BP_WORLD_RENDER_VERSION__=5368' site/world-v2300-map.js
          grep -Fq 'bridgepoint_frontend_status_v5000' site/app.js
          grep -Fq 'bridgepoint_public_building_detail_v5000' site/app.js
          grep -Fq 'bridgepoint_public_building_detail_by_id_v5024' site/app.js
          grep -Fq 'onBuildingSelect' site/app.js
          grep -Fq 'onBuildingSelect' site/world-v2300-map.js
          grep -Fq '__BP_V5000_SELECT_BUILDING__' site/app.js
          grep -Fq '__BP_V5000_SELECT_BUILDING__' site/world-v2300-map.js
          grep -Fq 'bridgepoint_public_context_v5003' site/app.js
          grep -Fq 'bridgepoint_public_weather_bootstrap_v5004' site/app.js
          grep -Fq 'bridgepoint_public_building_parts_v5000' site/app.js
          grep -Fq 'bridgepoint_public_foundation_sample_v5011' site/app.js
          grep -Fq 'bridgepoint_public_opportunity_viewport_v5025' site/app.js
          grep -Fq 'gta-place-label' site/world-v2300-map.js
          grep -Fq 'gta-road-label-major' site/world-v2300-map.js
          grep -Fq 'gta-poi-probe' site/world-v2300-map.js
          grep -Fq 'gta-poi-label' site/world-v2300-map.js
          grep -Fq 'gta-opportunity-buildings' site/world-v2300-map.js
          grep -Fq 'gta-opportunity-label' site/world-v2300-map.js
          grep -Fq 'interactionPriorityScheduling:true' site/world-v2300-map.js
          grep -Fq 'scheduleExact' site/world-v2300-map.js
          grep -Fq "Noto Sans Regular" site/world-v2300-map.js
          grep -Fq 'prefillBuildingFromFeature' site/app.js
          grep -Fq 'bFootprintArea' site/app.js
          grep -Fq 'bProvDataset' site/app.js
          grep -Fq 'gta-bp-roofs' site/world-v2300-map.js
          grep -Fq '__BP_BUILDING_MATERIALS__' site/world-v2300-map.js
          grep -Fq "mode:'structural-grey-3d'" site/world-v2300-map.js
          grep -Fq "id:'gta-context-floor-lines'" site/world-v2300-map.js
          grep -Fq "id:'gta-exact-floor-lines'" site/world-v2300-map.js
          grep -Fq "id:'gta-exact-roof'" site/world-v2300-map.js
          grep -Fq 'floorLines:true' site/world-v2300-map.js
          grep -Fq 'exactRoofs:true' site/world-v2300-map.js
          grep -Fq 'sourceBackedHeight:true' site/world-v2300-map.js
          grep -Fq 'bridgePointGeometry:true' site/world-v2300-map.js
          grep -Fq 'buildingParts:true' site/world-v2300-map.js
          grep -Fq 'integratedFloorBands:true' site/world-v2300-map.js
          grep -Fq 'realRoofTruthGeometry:true' site/world-v2300-map.js
          grep -Fq "roofTruthSourceLayer:'roofs'" site/world-v2300-map.js
          grep -Fq "'source-layer':'roofs'" site/world-v2300-map.js
          grep -Fq "const floorPattern=['step',['zoom'],'bp-grey-solid'" site/world-v2300-map.js
          grep -Fq "endpoint:'bridgepoint-public-building-tile-v5368'" site/app.js
          grep -Fq 'roofTruthGeometry:true' site/app.js
          grep -Fq "id:'gta-bp-floor-lines'" site/world-v2300-map.js
          grep -Fq "authoritative:useBp?'BRIDGEPOINT_STRUCTURAL':'GLOBAL_CONTEXT_STRUCTURAL'" site/world-v2300-map.js
          grep -Fq "actualHeightSource:useBp?'render_height_m':'OPENFREEMAP_HEIGHT'" site/world-v2300-map.js
          grep -Fq "roofMode:useBp?'BRIDGEPOINT_ROOF_TRUTH_GEOMETRY'" site/world-v2300-map.js
          grep -Fq 'minPitch:58,maxPitch:85' site/world-v2300-map.js
          grep -Fq "mode:'HORIZON_FREE_FLY_SINGLE_VIEW'" site/world-v2300-map.js
          grep -Fq 'horizonLocked:true' site/world-v2300-map.js
          grep -Fq "displaySource:'BRIDGEPOINT_STRUCTURAL_WITH_GLOBAL_FALLBACK'" site/app.js
          grep -Fq 'bridgePointHandoff:true' site/app.js
          grep -Fq 'maxPitch:85' site/world-v2300-map.js
          grep -Fq 'autoViewSwitching:false' site/world-v2300-map.js
          grep -Fq 'streetWalkMode:false' site/world-v2300-map.js
          ! grep -Fq 'installWalkUI();' site/world-v2300-map.js
          ! grep -Fq 'world.enterWalk?.()' site/app.js
          ! grep -Fq 'id="pViewStreet"' site/app.js
          ! grep -Fq 'id="pView3D"' site/app.js
          ! grep -Fq 'id="pViewAerial"' site/app.js
          grep -Fq 'bridgepoint-public-building-tile-v5368' site/world-v2300-map.js
          grep -Fq "'fill-extrusion-base':0" site/world-v2300-map.js
          ! grep -Fq "map.on('idle',syncBuildingShells)" site/world-v2300-map.js
          grep -Fq 'cacheBusting:false' site/app.js
          grep -Fq 'disabled_by_owner_v5368' site/app.js
          grep -Fq "ensureWeatherRasterPair('Radar')" site/app.js
          ! grep -Fq "ensureWeatherRasterPair('Cloud')" site/app.js
          ! grep -Fq 'bridgepoint-noaa-cloud-frames-v215' site/app.js
          grep -Fq 'atmosphereFogClouds:false' site/world-v2300-weather.js
          grep -Fq 'gta-bp-facade-detail' site/world-v2300-map.js
          grep -Fq 'gta-context-facade-detail' site/world-v2300-map.js
          grep -Fq 'gta-exact-facade-detail' site/world-v2300-map.js
          grep -Fq 'gta-context-building-footprints' site/world-v2300-map.js
          grep -Fq 'semanticBuildTimer' site/app.js
          grep -Fq 'semantic_build_stationary_recovery' site/app.js
          grep -Fq 'gta-context-building-parts' site/world-v2300-map.js
          grep -Fq "build_deferred_for_motion" site/app.js
          grep -Fq '__BP_EXACT_DETAIL_STATE__' site/world-v2300-map.js
          grep -Fq 'streetLevelClamped' site/world-v2300-map.js
          grep -Fq 'gta-bp-landmark3d-body' site/world-v2300-map.js
          grep -Fq 'cancelPendingTileRequestsWhileZooming:MOBILE' site/world-v2300-map.js
          grep -Fq 'wx-radar-haze' site/world-v2300-weather.js
          grep -Fq 'NOHRSC_Snow_Analysis' site/world-v2300-weather.js
          ! grep -Fq "map.getSource('selected')?.setData" site/world-v2300-map.js
          grep -Fq 'transportArchive' site/app.js
          grep -Fq 'transportRuntime' site/app.js
          grep -Fq 'bridgepoint-transport-runtime-broker-v2716' site/app.js
          grep -Fq 'bp-v2716-transport-segments' site/app.js
          test -s site/vendor/pmtiles.js
          grep -Fq 'BP_LANDING_V5050' site/index.html
          grep -Fq 'authModal' site/index.html
          grep -Fq 'authLegalAccept' site/index.html
          grep -Fq 'authPasswordToggle' site/index.html
          grep -Fq 'data-preview-cap="xray"' site/index.html
          grep -Fq 'data-preview-cap="imagery"' site/index.html
          grep -Fq 'bp_legal_accept_v1' site/app.js
          grep -Fq 'authLegalAccept' site/app/index.html
          test -s site/terms/index.html
          test -s site/privacy/index.html
          grep -Fq 'Terms of Use' site/terms/index.html
          grep -Fq 'Privacy Notice' site/privacy/index.html
          grep -Fq 'toggleLiveContext' site/app/index.html
          grep -Fq 'bottomNav' site/app/index.html
          grep -Fq 'bindPerformanceGovernor' site/app.js
          grep -Fq 'BP_MOBILE' site/app.js
          grep -Fq 'scheduleOpportunityBuildings' site/app.js
          grep -Fq 'MOBILE?650' site/world-v2300-map.js
          grep -Fq 'bpLivingWorld' site/world-v2300-map.js
          grep -Fq 'scheduleLivingWorld' site/world-v2300-map.js
          grep -Fq 'if(on){' site/world-v2300-map.js
          ! grep -Fq 'heavyMotion' site/world-v2300-map.js
          test -s site/app/horizon/index.html
          test -s site/current-map-entry.js
          test -s site/founder-access/index.html
          test -s site/for-roofers/index.html
          test -s site/for-solar/index.html
          test -s site/sample/index.html
          test -s site/owner/acquisition/index.html
          test -s site/owner/command/index.html
          test -s site/owner-everything.js
          test -s site/owner-everything.css
          test -s site/downloads/backup/bridgepoint_full_export_windows.ps1
          node --check site/owner-everything.js
          grep -Fq 'Owner Everything' site/app/index.html
          grep -Fq 'bridgepoint_owner_acquisition_v5400' site/owner-everything.js
          grep -Fq 'bridgepoint_owner_backend_section_v5402' site/owner-everything.js
          grep -Fq 'bridgepoint_owner_document_v5401' site/owner-everything.js
          grep -Fq 'BILLING OFF' site/founder-access/index.html
          grep -Fq 'founder-access.js' site/for-roofers/index.html
          ! grep -Fq 'bridgepoint-territory-report-v674' site/sample/index.html
          test -s site/discover/index.html
          test -s site/newsroom/index.html
          test -s site/newsroom/manifest.json
          test -s site/claims-terminal/index.html
          test -s site/claims-terminal/claims-lifecycle.json
          test -s site/claims-terminal/feed.xml
          test -s site/terminal/index.html
          test -s site/terminal/terminal.js
          test -s site/terminal/terminal.css
          test -s site/integrity-index/index.html
          test -s site/integrity-index/integrity.js
          test -s site/integrity-index/contract.json
          test -s site/settlement-lab/index.html
          test -s site/settlement-lab/settlement.js
          test -s site/settlement-lab/contract.json
          test -s site/distribution/index.html
          test -s site/distribution/gis-widget/index.html
          test -s site/distribution/gis-widget/widget.js
          test -s site/distribution/gis-widget/embed.js
          test -s site/distribution/openapi.json
          test -s site/distribution/telemetry.js
          test -s site/distribution/partner-delivery-contract.json
          node --check site/distribution/telemetry.js
          test -s site/distribution/bii-browser-extension.zip
          test -s site/distribution/qgis-plugin.zip
          unzip -l site/distribution/qgis-plugin.zip | grep -Fq 'bridgepoint_intelligence/__init__.py'
          unzip -l site/distribution/qgis-plugin.zip | grep -Fq 'bridgepoint_intelligence/metadata.txt'
          unzip -l site/distribution/qgis-plugin.zip | grep -Fq 'bridgepoint_intelligence/LICENSE'
          echo 'QGIS_PACKAGE_STRUCTURE_PASS'
          test -s site/public-audit/index.html
          test -s site/public-audit/audit.js
          test -s site/public-audit/contract.json
          test -s site/distribution/institutional-ingestion.html
          node --check site/public-audit/audit.js
          test -s site/portfolio-evidence/index.html
          test -s site/portfolio-evidence/app.js
          test -s site/portfolio-evidence/contract.json
          node --check site/portfolio-evidence/app.js
          test -s site/data-discrepancy/index.html
          test -s site/data-discrepancy/app.js
          test -s site/data-discrepancy/contract.json
          node --check site/data-discrepancy/app.js
          node --check site/distribution/gis-widget/widget.js
          node --check site/distribution/gis-widget/embed.js
          node --check site/distribution/bii-browser-extension/popup.js
          grep -Fq '/current-map-entry.js?v=5100' site/discover/index.html
          test -s site/app/horizon-playable/v2-entry.html
          test -s site/app/horizon-playable/horizon-v2.js
          test -s site/app/horizon-playable/assets/characters/mesh2motion/models/swat_male.glb
          echo "V5000 CLEAN INTELLIGENCE ARTIFACT READY"

      - name: Persist successful V5000 artifact as durable live baseline
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/v5000-live
          mkdir -p /tmp/v5000-live
          cp -a site/. /tmp/v5000-live/
          cd /tmp/v5000-live
          git init -q
          git config user.name 'BridgePoint V5000 Production Authority'
          git config user.email 'actions@users.noreply.github.com'
          git checkout -b live-artifact
          git add -A
          git commit -m "Publish successful BridgePoint V5000 ${GITHUB_SHA}"
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push --force origin live-artifact

      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v4
        with:
          name: bridgepoint-v5000-${{ github.run_attempt }}
          path: site

      - name: Deploy clean V5000 surface
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: bridgepoint-v5000-${{ github.run_attempt }}

      - name: Verify production cutover
        shell: bash
        run: |
          set -euo pipefail
          O='https://bridgepointintelligence.online'
          ok=0
          for i in $(seq 1 45); do
            q="${GITHUB_SHA}-${i}-$(date +%s)"
            curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/?v5050=${q}" -o /tmp/root.html || true
            curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/app/?v5050=${q}" -o /tmp/app.html || true
            curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/app.js?v5368=${q}" -o /tmp/app.js || true
            curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/world-v2300-map.js?v5368=${q}" -o /tmp/world-v2300-map.js || true
            if grep -Fq 'BP_LANDING_V5050' /tmp/root.html && grep -Fq 'authModal' /tmp/root.html && grep -Fq 'app.js?v=5368' /tmp/app.html && grep -Fq 'BP_FRONTEND_V5000' /tmp/app.html && grep -Fq 'toggleLiveContext' /tmp/app.html && grep -Fq '__BP_APP_BUILD_VERSION__=5368' /tmp/app.js && grep -Fq '__BP_WORLD_RENDER_VERSION__=5368' /tmp/world-v2300-map.js && grep -Fq 'bridgepoint_frontend_status_v5000' /tmp/app.js && grep -Fq 'bindPerformanceGovernor' /tmp/app.js && grep -Fq 'bridgepoint_public_weather_bootstrap_v5004' /tmp/app.js; then ok=1; break; fi
            sleep 4
          done
          test "$ok" = 1
          grep -Fq 'BRIDGEPOINT' /tmp/root.html
          ! grep -Eiq 'HORIZON|battle pass|zombie|game mode' /tmp/root.html
          q="${GITHUB_SHA}-surface-check-$(date +%s)"
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/app/horizon/?v5100=${q}" -o /tmp/horizon.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/app/horizon-playable/v2-entry.html?v5100=${q}" -o /tmp/horizon-playable.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/discover/?v5100=${q}" -o /tmp/discover.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/newsroom/?v5100=${q}" -o /tmp/newsroom.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/claims-terminal/?v5100=${q}" -o /tmp/claims-terminal.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/terminal/?v5100=${q}" -o /tmp/terminal.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/integrity-index/?v5100=${q}" -o /tmp/integrity-index.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/settlement-lab/?v5100=${q}" -o /tmp/settlement-lab.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/distribution/?v5202=${q}" -o /tmp/distribution.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/distribution/gis-widget/?v5202=${q}" -o /tmp/gis-widget.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/public-audit/?v5203=${q}" -o /tmp/public-audit.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/portfolio-evidence/?v5204=${q}" -o /tmp/portfolio-evidence.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/data-discrepancy/?v5208=${q}" -o /tmp/data-discrepancy.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "https://xdfsjztwgsbmabshzsjw.supabase.co/functions/v1/bridgepoint-public-exposure-feed-v5202?format=json&limit=3" -o /tmp/bridgepoint-exposure-feed.json
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "https://xdfsjztwgsbmabshzsjw.supabase.co/functions/v1/bridgepoint-public-exposure-feed-v5202?format=rss&limit=3" -o /tmp/bridgepoint-exposure-feed.rss
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/founder-access/?v5100=${q}" -o /tmp/founder.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/for-solar/?v5100=${q}" -o /tmp/solar.html
          curl --retry 3 --retry-all-errors --retry-delay 1 -fsSL -H 'Cache-Control: no-cache' "${O}/sample/?v5100=${q}" -o /tmp/sample.html
          test -s /tmp/horizon.html
          test -s /tmp/horizon-playable.html
          grep -Fq 'Live Match' /tmp/horizon-playable.html
          test -s /tmp/discover.html
          test -s /tmp/newsroom.html
          test -s /tmp/claims-terminal.html
          test -s /tmp/terminal.html
          test -s /tmp/integrity-index.html
          test -s /tmp/settlement-lab.html
          test -s /tmp/distribution.html
          test -s /tmp/gis-widget.html
          test -s /tmp/public-audit.html
          test -s /tmp/portfolio-evidence.html
          test -s /tmp/data-discrepancy.html
          test -s /tmp/bridgepoint-exposure-feed.json
          test -s /tmp/bridgepoint-exposure-feed.rss
          test -s /tmp/founder.html
          test -s /tmp/solar.html
          test -s /tmp/sample.html
          grep -Fq 'BILLING OFF' /tmp/founder.html
          grep -Fq 'Property intelligence for solar teams' /tmp/solar.html
          ! grep -Fq 'bridgepoint-territory-report-v674' /tmp/sample.html
          grep -Fq 'GROWTH NEWSROOM' /tmp/newsroom.html
          grep -Fq 'Claims Lifecycle Terminal' /tmp/claims-terminal.html
          grep -Fq 'Institutional Terminal' /tmp/terminal.html
          grep -Fq 'BridgePoint Integrity Index' /tmp/integrity-index.html
          grep -Fq 'BridgePoint Settlement Lab' /tmp/settlement-lab.html
          grep -Fq 'Distribution Network' /tmp/distribution.html
          grep -Fq 'BridgePoint GIS Property Widget' /tmp/gis-widget.html
          grep -Fq 'Public Evidence Audit' /tmp/public-audit.html
          grep -Fq 'Portfolio Evidence Lab' /tmp/portfolio-evidence.html
          grep -Fq 'Data Discrepancy Ledger' /tmp/data-discrepancy.html
          grep -Fq 'BridgePoint Exposure Stream' /tmp/bridgepoint-exposure-feed.json
          grep -Fq '"trade_instruction": false' /tmp/bridgepoint-exposure-feed.json
          grep -Fq '<title>BridgePoint Exposure Stream</title>' /tmp/bridgepoint-exposure-feed.rss
          grep -Fq 'BRIDGEPOINT' /tmp/horizon.html
          grep -Fq 'current-map-entry.js?v=5100' /tmp/discover.html
          echo BRIDGEPOINT_V5100_COMBINED_SURFACES_PASS

      - name: Android smoke
        shell: bash
        run: |
          set -euo pipefail
          CHROME=$(command -v google-chrome || command -v chromium-browser || command -v chromium)
          cd /tmp
          npm init -y >/dev/null 2>&1 || true
          npm install puppeteer-core@24 --no-audit --no-fund >/dev/null 2>&1
          CHROME_PATH="$CHROME" node <<'NODE'
          const puppeteer=require('puppeteer-core');
          (async()=>{
            const browser=await puppeteer.launch({headless:true,executablePath:process.env.CHROME_PATH,args:['--no-sandbox','--disable-dev-shm-usage','--enable-webgl','--ignore-gpu-blocklist','--enable-unsafe-swiftshader']});
            try{
              const p=await browser.newPage();
              await p.setUserAgent('Mozilla/5.0 (Linux; Android 16; Pixel 9a) AppleWebKit/537.36 Chrome/140 Mobile Safari/537.36');
              await p.setViewport({width:390,height:844,deviceScaleFactor:2,isMobile:true,hasTouch:true});
              const errors=[];p.on('pageerror',e=>errors.push(String(e.message||e)));
              await p.goto('https://bridgepointintelligence.online/app/?qa=v5368-'+Date.now(),{waitUntil:'domcontentloaded',timeout:30000});
              await p.waitForFunction(()=>window.__BP_FRONTEND_VERSION===5000&&window.__BP_APP_BUILD_VERSION__===5368,{timeout:20000});
              await p.waitForFunction(()=>window.__BP_WORLD_RENDER_VERSION__===5368,{timeout:30000});
              await p.waitForFunction(()=>document.getElementById('canonicalProperties')?.textContent?.includes(','),{timeout:15000});
              await p.waitForFunction(()=>document.querySelectorAll('#liveMap canvas').length===1,{timeout:25000});
              await p.waitForFunction(()=>!!document.getElementById('bpTabContext')&&!!document.getElementById('bXrayUnderground')&&!!document.getElementById('toggleLiveContext')&&!!document.getElementById('ctxSurfaceMaterial')&&!!document.getElementById('ctxSurfaceSource')&&!!document.getElementById('transportArchive')&&!!document.getElementById('transportRuntime'),{timeout:10000});
              await p.waitForFunction(()=>!!window.__BP_V5000_WORLD?.map?.getLayer('bp-v5004-weather-points'),{timeout:90000});
              const weatherVisualState=await p.evaluate(()=>{const m=window.__BP_V5000_WORLD?.map;return{
                radarA:!!m?.getLayer('bp-radar-a'),radarB:!!m?.getLayer('bp-radar-b'),
                points:!!m?.getLayer('bp-v5004-weather-points'),
                cloudA:!!m?.getLayer('bp-cloud-a'),cloudB:!!m?.getLayer('bp-cloud-b'),
                cloudState:window.__BP_CLOUD_STATE__||null
              }});
              if(!weatherVisualState.radarA||!weatherVisualState.radarB||!weatherVisualState.points||weatherVisualState.cloudA||weatherVisualState.cloudB||weatherVisualState.cloudState?.enabled!==false)throw new Error('WEATHER_VISUAL_CONTRACT '+JSON.stringify(weatherVisualState));
              console.log('BRIDGEPOINT_RADAR_DOTS_NO_CLOUD_PASS',JSON.stringify(weatherVisualState));
              await p.waitForFunction(()=>{
                const m=window.__BP_V5000_WORLD?.map;
                return !!m?.getLayer('gta-place-label')&&!!m?.getLayer('gta-road-label-major')&&!!m?.getLayer('gta-poi-label')&&!!m?.getLayer('gta-opportunity-buildings')&&!!m?.getLayer('gta-opportunity-label');
              },{timeout:15000});
              const launch=await p.evaluate(()=>{
                const m=window.__BP_V5000_WORLD?.map;
                const lang=document.querySelector('.topbar-actions > .bp-language--topbar select');
                return {
                  center:m?[m.getCenter().lng,m.getCenter().lat]:null,
                  zoom:m?.getZoom?.()??null,
                  languageTopbar:!!lang,
                  weatherMinZoom:m?.getLayer('bp-v5004-weather-points')?.minzoom??99,
                  pitch:m?.getPitch?.()??null,
                  minPitch:m?.getMinPitch?.()??null,
                  maxPitch:m?.getMaxPitch?.()??null
                };
              });
              if(!launch.languageTopbar||!launch.center||Math.abs(launch.center[0]+98.5)>2||Math.abs(launch.center[1]-39.5)>2||launch.zoom>3.6||launch.weatherMinZoom>3.6||Number(launch.pitch)<57.5||Number(launch.minPitch)<57.5||Number(launch.maxPitch)<84)throw new Error('NATIONAL_LAUNCH_CONTRACT '+JSON.stringify(launch));
              await p.evaluate(()=>{
                const m=window.__BP_V5000_WORLD?.map;
                m?.jumpTo({center:[-98.5,39.5],zoom:4.2,pitch:68,bearing:0});
              });
              await p.waitForFunction(()=>{
                const m=window.__BP_V5000_WORLD?.map;
                if(!m)return false;
                try{return m.queryRenderedFeatures({layers:['gta-place-label']}).length>0}catch{return false}
              },{timeout:20000});
              console.log('BRIDGEPOINT_TEXT_LABELS_RENDERED',JSON.stringify(launch));
              const startState=await p.evaluate(()=>({
                center:[window.__BP_V5000_WORLD?.map?.getCenter?.().lng,window.__BP_V5000_WORLD?.map?.getCenter?.().lat],
                zoom:window.__BP_V5000_WORLD?.map?.getZoom?.(),
                pitch:window.__BP_V5000_WORLD?.map?.getPitch?.(),
                minPitch:window.__BP_V5000_WORLD?.map?.getMinPitch?.(),
                langTopbar:!!document.querySelector('.topbar-actions .bp-language--topbar'),
                langBottom:!!document.querySelector('body>.bp-language:not(.bp-language--topbar)'),
                weatherMin:[window.__BP_V5000_WORLD?.map?.getLayer('bp-v5004-weather-points')?.minzoom,window.__BP_V5000_WORLD?.map?.getLayer('bp-v5004-weather-shape-line')?.minzoom]
              }));
              if(!startState.langTopbar||startState.langBottom)throw new Error('APP_LANGUAGE_POSITION_REGRESSION '+JSON.stringify(startState));
              if(!Number.isFinite(startState.zoom)||startState.zoom>4.2||Math.abs(startState.center[0]+98.5)>8||Math.abs(startState.center[1]-39.5)>7||Number(startState.pitch)<57.5||Number(startState.minPitch)<57.5)throw new Error('APP_NATIONAL_START_REGRESSION '+JSON.stringify(startState));
              if(startState.weatherMin.some(x=>Number(x)>2.2))throw new Error('APP_NATIONAL_WEATHER_MINZOOM_REGRESSION '+JSON.stringify(startState));
              console.log('BRIDGEPOINT_APP_NATIONAL_START_PASS',JSON.stringify(startState));
              await p.evaluate(()=>{
                const m=window.__BP_V5000_WORLD?.map;
                if(!m)throw new Error('semantic map unavailable');
                m.jumpTo({center:[-73.9857,40.7484],zoom:16.4,pitch:52,bearing:0});
                setTimeout(()=>window.__BP_REFRESH_SEMANTIC_LABELS__?.(),700);
              });
              try{
                await p.waitForFunction(()=>{
                  const s=window.__BP_SEMANTIC_STATS;
                  return Number(s?.features||0)>0&&Number(s?.anchored||0)>0;
                },{timeout:30000});
              }catch(e){
                console.log('BRIDGEPOINT_SEMANTIC_TIMEOUT_STATE',await p.evaluate(()=>JSON.stringify({stats:window.__BP_SEMANTIC_STATS||null,debug:window.__BP_SEMANTIC_DEBUG__||null,shell:window.__BP_BUILDING_SHELL_MODE__||null,center:window.__BP_V5000_WORLD?.map?[window.__BP_V5000_WORLD.map.getCenter().lng,window.__BP_V5000_WORLD.map.getCenter().lat]:null,zoom:window.__BP_V5000_WORLD?.map?.getZoom?.()||null})));
                throw e
              }
              console.log('BRIDGEPOINT_SEMANTIC_BUILDING_LABELS_PASS',await p.evaluate(()=>JSON.stringify(window.__BP_SEMANTIC_STATS||{})));
              const persistenceState=await p.evaluate(async()=>{
                const m=window.__BP_V5000_WORLD?.map;
                if(!m)throw new Error('persistent renderer unavailable');
                const layers=['gta-bp-buildings','gta-context-buildings','gta-road-major','gta-road-local','gta-landcover'].filter(id=>m.getLayer(id));
                const before=Object.fromEntries(layers.map(id=>[id,m.getLayoutProperty(id,'visibility')||'visible']));
                m.easeTo({center:[-72.92,41.31],zoom:15.2,duration:420});
                await new Promise(r=>setTimeout(r,100));
                const during=Object.fromEntries(layers.map(id=>[id,m.getLayoutProperty(id,'visibility')||'visible']));
                await new Promise(r=>setTimeout(r,520));
                const after=Object.fromEntries(layers.map(id=>[id,m.getLayoutProperty(id,'visibility')||'visible']));
                return {before,during,after,light:window.__BP_WORLD_LIGHT__||null,living:window.__BP_LIVING_WORLD_STATE__||null}
              });
              console.log('BRIDGEPOINT_PERSISTENT_RENDERER',JSON.stringify(persistenceState));
              const visible=v=>v!=='none';
              const shellVisible=s=>visible(s['gta-bp-buildings'])||visible(s['gta-context-buildings']);
              for(const phase of ['before','during','after']){
                const s=persistenceState[phase]||{};
                if(!shellVisible(s)||!visible(s['gta-road-major'])||!visible(s['gta-road-local'])||!visible(s['gta-landcover']))throw new Error('PERSISTENT_RENDERER_BLANKED_AUTHORITATIVE_LAYER '+phase+' '+JSON.stringify(persistenceState));
              }

              // Regression gate for the exact blank-building failure seen in the public preview:
              // suburban U.S. viewports must keep the global shell visible until BridgePoint
              // building tiles are loaded and actually contain features.
              await p.evaluate(()=>{
                const m=window.__BP_V5000_WORLD?.map;if(!m)throw new Error('suburban fallback map unavailable');
                m.jumpTo({center:[-74.0926,40.8987],zoom:16.1,pitch:50,bearing:0});
              });
              await p.waitForFunction(()=>{
                const m=window.__BP_V5000_WORLD?.map;if(!m)return false;
                const ids=['gta-bp-buildings','gta-context-buildings'].filter(id=>m.getLayer(id)&&(m.getLayoutProperty(id,'visibility')||'visible')!=='none');
                try{return ids.length>0&&ids.some(id=>m.queryRenderedFeatures({layers:[id]}).length>0)}catch{return false}
              },{timeout:30000});
              const suburbanShell=await p.evaluate(()=>{
                const m=window.__BP_V5000_WORLD?.map;
                const mode=window.__BP_BUILDING_SHELL_MODE__||{};
                const rendered={};
                for(const id of ['gta-bp-buildings','gta-context-buildings']){
                  try{rendered[id]=m?.getLayer(id)&&(m.getLayoutProperty(id,'visibility')||'visible')!=='none'?m.queryRenderedFeatures({layers:[id]}).length:0}catch{rendered[id]=0}
                }
                return{mode,rendered}
              });
              if(Object.values(suburbanShell.rendered||{}).reduce((a,b)=>a+Number(b||0),0)<1)throw new Error('SUBURBAN_BUILDING_FALLBACK_BLANK '+JSON.stringify(suburbanShell));
              console.log('BRIDGEPOINT_SUBURBAN_BUILDING_FALLBACK_PASS',JSON.stringify(suburbanShell));

              const frameProbe=await p.evaluate(async()=>{
                const m=window.__BP_V5000_WORLD?.map;if(!m)throw new Error('frame probe map missing');
                const gaps=[];let active=true,last=performance.now();
                const tick=t=>{gaps.push(t-last);last=t;if(active)requestAnimationFrame(tick)};
                requestAnimationFrame(tick);
                const moves=[
                  {center:[-72.90,41.30],zoom:15.7,pitch:56,bearing:-14},
                  {center:[-72.89,41.305],zoom:16.1,pitch:62,bearing:12},
                  {center:[-72.91,41.298],zoom:15.5,pitch:48,bearing:-26}
                ];
                for(const x of moves){m.easeTo({...x,duration:520});await new Promise(r=>setTimeout(r,650))}
                await new Promise(r=>setTimeout(r,700));active=false;
                const sorted=gaps.filter(x=>Number.isFinite(x)&&x>0).sort((a,b)=>a-b);
                const p95=sorted.length?sorted[Math.min(sorted.length-1,Math.floor(sorted.length*.95))]:0;
                return {frames:sorted.length,maxGapMs:sorted.length?sorted[sorted.length-1]:0,p95GapMs:p95}
              });
              console.log('BRIDGEPOINT_MOTION_FRAME_PROBE',JSON.stringify(frameProbe));
              if(frameProbe.maxGapMs>450)console.warn('MAP_FRAME_OUTLIER',JSON.stringify(frameProbe)); if(frameProbe.frames<30||frameProbe.p95GapMs>550||frameProbe.maxGapMs>800)throw new Error('MAP_FRAME_STALL '+JSON.stringify(frameProbe));
              await p.waitForFunction(()=>{const t=document.getElementById('liveLegendCount')?.textContent;return !!t&&t!=='CHECKING'}, {timeout:20000});
              await p.waitForFunction(()=>getComputedStyle(document.getElementById('systemPanel')).display==='none',{timeout:5000});
              console.log('BRIDGEPOINT_MOBILE_STATUS_PANEL_CLOSED');

              // Regression gate: the authoritative visible shell must be selectable.
              await p.evaluate(()=>{
                const m=window.__BP_V5000_WORLD?.map;
                if(!m)throw new Error('authoritative-hit-test map unavailable');
                m.jumpTo({center:[-73.9857,40.7484],zoom:17.2,pitch:58,bearing:0});
              });
              await p.waitForFunction(()=>{
                const m=window.__BP_V5000_WORLD?.map,mode=window.__BP_BUILDING_SHELL_MODE__;
                if(!m||mode?.authoritative!=='BRIDGEPOINT_STRUCTURAL'||mode?.bridgePointCoverageReady!==true||Number(mode?.bridgePointRendered||0)<10)return false;
                if(!m.getLayer('gta-bp-buildings')||(m.getLayoutProperty('gta-bp-buildings','visibility')||'visible')==='none')return false;
                try{return m.queryRenderedFeatures({layers:['gta-bp-buildings']}).length>=10}catch{return false}
              },{timeout:45000});
              const structuralProof=await p.evaluate(()=>{
                const m=window.__BP_V5000_WORLD?.map,fs=m?.queryRenderedFeatures({layers:['gta-bp-buildings']})||[];
                const heights=fs.map(f=>Number(f?.properties?.render_height_m)).filter(Number.isFinite);
                const floors=fs.map(f=>Number(f?.properties?.resolved_floors)).filter(Number.isFinite);
                const unique=[...new Set(heights.map(x=>x.toFixed(2)))];
                const parts=(()=>{try{return m?.getLayer('gta-context-building-parts')&&(m.getLayoutProperty('gta-context-building-parts','visibility')||'visible')!=='none'?m.queryRenderedFeatures({layers:['gta-context-building-parts']}).length:0}catch{return 0}})();
                const roofs=(()=>{try{return m?.getLayer('gta-bp-roofs')&&(m.getLayoutProperty('gta-bp-roofs','visibility')||'visible')!=='none'?m.queryRenderedFeatures({layers:['gta-bp-roofs']}).length:0}catch{return 0}})();
                const pattern=m?.getPaintProperty('gta-bp-buildings','fill-extrusion-pattern');
                const floorPatternActive=JSON.stringify(pattern||null).includes('bp-floor-lines')&&!!m?.hasImage?.('bp-floor-lines')&&!!m?.hasImage?.('bp-grey-solid');
                const roofLayer=m?.getLayer('gta-bp-roofs');
                return {mode:window.__BP_BUILDING_SHELL_MODE__||{},rendered:fs.length,heightCount:heights.length,uniqueHeights:unique.length,minHeight:heights.length?Math.min(...heights):null,maxHeight:heights.length?Math.max(...heights):null,resolvedFloorCount:floors.length,maxResolvedFloors:floors.length?Math.max(...floors):null,parts,roofs,floorPatternActive,roofSourceLayer:roofLayer?.['source-layer']||null};
              });
              if(structuralProof.rendered<10||structuralProof.heightCount<10||structuralProof.uniqueHeights<4||!(structuralProof.maxHeight-structuralProof.minHeight>10)||structuralProof.resolvedFloorCount<10||!structuralProof.floorPatternActive||structuralProof.roofs<1||structuralProof.roofSourceLayer!=='roofs')throw new Error('STRUCTURAL_BUILDING_PROOF_FAILED '+JSON.stringify(structuralProof));
              console.log('BRIDGEPOINT_STRUCTURAL_BUILDINGS_PASS',JSON.stringify(structuralProof));
              const freeFlyDetail=await p.evaluate(()=>({
                camera:window.__BP_CAMERA_MODE__||null,
                pitch:window.__BP_V5000_WORLD?.map?.getPitch?.()??null,
                minPitch:window.__BP_V5000_WORLD?.map?.getMinPitch?.()??null,
                maxPitch:window.__BP_V5000_WORLD?.map?.getMaxPitch?.()??null,
                integratedFloorPattern:(()=>{const m=window.__BP_V5000_WORLD?.map,p=m?.getPaintProperty('gta-bp-buildings','fill-extrusion-pattern');return JSON.stringify(p||null).includes('bp-floor-lines')&&!!m?.hasImage?.('bp-floor-lines')&&!!m?.hasImage?.('bp-grey-solid')})(),
                dedicatedFloorOverlayHidden:(()=>{const m=window.__BP_V5000_WORLD?.map;return !!m?.getLayer('gta-bp-floor-lines')&&(m.getLayoutProperty('gta-bp-floor-lines','visibility')||'visible')==='none'})(),
                roofSourceLayer:(()=>window.__BP_V5000_WORLD?.map?.getLayer('gta-bp-roofs')?.['source-layer']||null)(),
                partsVisible:(()=>{const m=window.__BP_V5000_WORLD?.map;return !!m?.getLayer('gta-context-building-parts')&&(m.getLayoutProperty('gta-context-building-parts','visibility')||'visible')!=='none'})(),
                bpRoofVisible:(()=>{const m=window.__BP_V5000_WORLD?.map;return !!m?.getLayer('gta-bp-roofs')&&(m.getLayoutProperty('gta-bp-roofs','visibility')||'visible')!=='none'})(),
                exactBodyLayer:!!window.__BP_V5000_WORLD?.map?.getLayer('gta-exact-building'),
                exactFloorLayer:!!window.__BP_V5000_WORLD?.map?.getLayer('gta-exact-floor-lines'),
                exactRoofLayer:!!window.__BP_V5000_WORLD?.map?.getLayer('gta-exact-roof'),
                walkButton:!!document.getElementById('bpWalkToggle'),
                aerialButton:!!document.getElementById('pViewAerial'),
                view3dButton:!!document.getElementById('pView3D'),
                streetButton:!!document.getElementById('pViewStreet')
              }));
              if(freeFlyDetail.camera?.mode!=='HORIZON_FREE_FLY_SINGLE_VIEW'||freeFlyDetail.camera?.singleView!==true||freeFlyDetail.camera?.autoViewSwitching!==false||freeFlyDetail.camera?.streetWalkMode!==false||freeFlyDetail.camera?.horizonLocked!==true||Number(freeFlyDetail.pitch)<57.5||Number(freeFlyDetail.minPitch)<57.5||Number(freeFlyDetail.maxPitch)<84||!freeFlyDetail.integratedFloorPattern||!freeFlyDetail.dedicatedFloorOverlayHidden||freeFlyDetail.roofSourceLayer!=='roofs'||!freeFlyDetail.partsVisible||!freeFlyDetail.bpRoofVisible||!freeFlyDetail.exactBodyLayer||!freeFlyDetail.exactFloorLayer||!freeFlyDetail.exactRoofLayer||freeFlyDetail.walkButton||freeFlyDetail.aerialButton||freeFlyDetail.view3dButton||freeFlyDetail.streetButton)throw new Error('HORIZON_STRUCTURAL_DETAIL_CONTRACT '+JSON.stringify(freeFlyDetail));
              console.log('BRIDGEPOINT_HORIZON_STRUCTURAL_DETAIL_PASS',JSON.stringify(freeFlyDetail));
              await p.waitForFunction(()=>{
                const m=window.__BP_V5000_WORLD?.map;
                if(!m)return false;
                const candidates=['gta-exact-building','gta-bp-buildings','gta-context-buildings'].filter(id=>m.getLayer(id)&&(m.getLayoutProperty(id,'visibility')||'visible')!=='none');
                try{return candidates.some(id=>m.queryRenderedFeatures({layers:[id]}).length>0)}catch{return false}
              },{timeout:45000});
              const cameraBeforeBuildingSelect=await p.evaluate(()=>{const m=window.__BP_V5000_WORLD?.map,c=m?.getCenter?.();return{lng:c?.lng,lat:c?.lat,zoom:m?.getZoom?.(),pitch:m?.getPitch?.(),bearing:m?.getBearing?.()}});
              const authoritativeBuildingHit=await p.evaluate(()=>{
                const w=window.__BP_V5000_WORLD,m=w?.map;
                if(!w?.selectBuildingAtPoint||!m)return null;
                const layers=['gta-exact-building','gta-bp-buildings','gta-context-buildings'].filter(id=>m.getLayer(id)&&(m.getLayoutProperty(id,'visibility')||'visible')!=='none');
                const cv=m.getCanvas(),width=cv.clientWidth||cv.width,height=cv.clientHeight||cv.height;
                for(let y=80;y<height-60;y+=18)for(let x=18;x<width-18;x+=18){
                  try{if(m.queryRenderedFeatures({x,y},{layers}).length&&w.selectBuildingAtPoint({x,y})===true)return{x,y,layers}}catch(_){}
                }
                return null;
              });
              if(!authoritativeBuildingHit)throw new Error('VISIBLE_AUTHORITATIVE_BUILDING_NOT_CLICKABLE');
              await p.waitForFunction(()=>document.getElementById('buildingPanel')?.hidden===false,{timeout:5000});
              console.log('BRIDGEPOINT_AUTHORITATIVE_BUILDING_HITTEST_PASS',JSON.stringify(authoritativeBuildingHit));
              const cameraAfterBuildingSelect=await p.evaluate(()=>{const m=window.__BP_V5000_WORLD?.map,c=m?.getCenter?.();return{lng:c?.lng,lat:c?.lat,zoom:m?.getZoom?.(),pitch:m?.getPitch?.(),bearing:m?.getBearing?.()}});
              const camDelta=Math.abs((cameraBeforeBuildingSelect.lng||0)-(cameraAfterBuildingSelect.lng||0))+Math.abs((cameraBeforeBuildingSelect.lat||0)-(cameraAfterBuildingSelect.lat||0))+Math.abs((cameraBeforeBuildingSelect.zoom||0)-(cameraAfterBuildingSelect.zoom||0))+Math.abs((cameraBeforeBuildingSelect.pitch||0)-(cameraAfterBuildingSelect.pitch||0))+Math.abs((cameraBeforeBuildingSelect.bearing||0)-(cameraAfterBuildingSelect.bearing||0));
              if(camDelta>0.001)throw new Error('BUILDING_SELECTION_MOVED_CAMERA '+JSON.stringify({cameraBeforeBuildingSelect,cameraAfterBuildingSelect,camDelta}));
              console.log('BRIDGEPOINT_BUILDING_SELECTION_CAMERA_STABLE_PASS',JSON.stringify(cameraAfterBuildingSelect));
              const structuralState=await p.evaluate(()=>({
                mode:window.__BP_BUILDING_SHELL_MODE__||null,
                materials:window.__BP_BUILDING_MATERIALS__||null,
                bpVisible:(()=>{const m=window.__BP_V5000_WORLD?.map;return !!m?.getLayer('gta-bp-buildings')&&(m.getLayoutProperty('gta-bp-buildings','visibility')||'visible')!=='none'})(),
                contextVisible:(()=>{const m=window.__BP_V5000_WORLD?.map;return !!m?.getLayer('gta-context-buildings')&&(m.getLayoutProperty('gta-context-buildings','visibility')||'visible')!=='none'})(),
                partsVisible:(()=>{const m=window.__BP_V5000_WORLD?.map;return !!m?.getLayer('gta-context-building-parts')&&(m.getLayoutProperty('gta-context-building-parts','visibility')||'visible')!=='none'})(),
                bpRoofVisible:(()=>{const m=window.__BP_V5000_WORLD?.map;return !!m?.getLayer('gta-bp-roofs')&&(m.getLayoutProperty('gta-bp-roofs','visibility')||'visible')!=='none'})(),
                refresh:window.__BP_BUILDING_TILE_REFRESH__||null
              }));
              if(structuralState.mode?.authoritative!=='BRIDGEPOINT_STRUCTURAL'||structuralState.mode?.bridgePointCoverageReady!==true||structuralState.mode?.floorLines!==true||structuralState.mode?.integratedFloorBands!==true||structuralState.mode?.buildingParts!==true||structuralState.mode?.roofReady!==true||structuralState.mode?.roofMode!=='BRIDGEPOINT_ROOF_TRUTH_GEOMETRY'||structuralState.mode?.cameraMode!=='HORIZON_FREE_FLY_SINGLE_VIEW'||structuralState.materials?.sourceBackedHeight!==true||structuralState.materials?.bridgePointGeometry!==true||structuralState.materials?.floorLines!==true||structuralState.materials?.integratedFloorBands!==true||structuralState.materials?.realRoofTruthGeometry!==true||structuralState.materials?.roofTruthSourceLayer!=='roofs'||structuralState.materials?.roofOverlays!==true||structuralState.materials?.buildingParts!==true||structuralState.bpVisible!==true||structuralState.contextVisible!==false||structuralState.partsVisible!==true||structuralState.bpRoofVisible!==true||structuralState.refresh?.bridgePointHandoff!==true||structuralState.refresh?.integratedFloorBands!==true||structuralState.refresh?.roofTruthGeometry!==true||structuralState.refresh?.endpoint!=='bridgepoint-public-building-tile-v5368')throw new Error('STRUCTURAL_BUILDING_STATE_INVALID '+JSON.stringify(structuralState));
              console.log('BRIDGEPOINT_STRUCTURAL_BUILDING_STATE_PASS',JSON.stringify(structuralState));
              await p.evaluate(()=>window.__BP_V5000_CLEAR_BUILDING__?.());

              await p.evaluate(()=>{
                const map=window.__BP_V5000_WORLD?.map;
                if(!map)throw new Error('map unavailable');
                const lng=-72.6714967515864,lat=41.7398844613305;
                map.jumpTo({center:[lng,lat],zoom:18,pitch:58,bearing:0});
                const feature={
                  type:'Feature',
                  id:88366380,
                  geometry:{type:'MultiPolygon',coordinates:[[[[-72.671624,41.74007],[-72.671574,41.739789],[-72.671389,41.739807],[-72.671417,41.739962],[-72.671029,41.74],[-72.671052,41.740126],[-72.671624,41.74007]]]]},
                  properties:{building_id:88366380,render_height_m:9.457785606384277,height_m:9.457785606384277,source_confidence:.9678999781608582,height_truth:'SOURCE_BACKED_HEIGHT'}
                };
                if(typeof window.__BP_V5000_SELECT_BUILDING__!=='function')throw new Error('PUBLIC_BUILDING_SELECTOR_MISSING');
                const selectorReturn=window.__BP_V5000_SELECT_BUILDING__({lngLat:{lng,lat},feature});
                if(selectorReturn!==true)throw new Error('PUBLIC_BUILDING_SELECTOR_RETURNED_FALSE');
              });
              await p.waitForFunction(()=>document.getElementById('buildingPanel')?.hidden===false,{timeout:5000});
              console.log('BRIDGEPOINT_BUILDING_CLICK_PANEL_OPEN','BACKEND_GEOMETRY');
              await p.waitForFunction(()=>{
                const vals=[
                  document.getElementById('bHeight')?.textContent,
                  document.getElementById('bFloors')?.textContent,
                  document.getElementById('bSource')?.textContent,
                  document.getElementById('bFootprintArea')?.textContent,
                  document.getElementById('bProvDataset')?.textContent
                ];
                return vals.every(v=>v&&v!=='—'&&!/^Resolving/i.test(v));
              },{timeout:15000});
              console.log('BRIDGEPOINT_BUILDING_CARD_POPULATED');

              try{
                await p.waitForFunction(()=>{
                  const m=window.__BP_V5000_WORLD?.map;
                  const d=m?.getSource('bpV5001SelectedParcel')?.serialize?.()?.data||window.__BP_SELECTED_PARCEL_SOURCE_DATA__;
                  const area=document.getElementById('bPickedParcelArea')?.textContent;
                  const state=window.__BP_PARCEL_CUTOUT_STATE__;
                  return state?.status==='ready'&&(d?.features?.length||0)===1&&area&&area!=='—'&&area!=='not linked';
                },{timeout:45000});
              }catch(e){
                console.log('BRIDGEPOINT_PARCEL_CUTOUT_TIMEOUT_STATE',JSON.stringify(await p.evaluate(()=>({
                  state:window.__BP_PARCEL_CUTOUT_STATE__||null,
                  area:document.getElementById('bPickedParcelArea')?.textContent,
                  selectedParcel:(()=>{const d=window.__BP_V5000_WORLD?.map?.getSource('bpV5001SelectedParcel')?.serialize?.()?.data||window.__BP_SELECTED_PARCEL_SOURCE_DATA__;return d||null})()
                }))));
                throw e;
              }
              console.log('BRIDGEPOINT_PARCEL_CUTOUT_RENDERED',JSON.stringify(await p.evaluate(()=>window.__BP_PARCEL_CUTOUT_STATE__||{})));

              await p.waitForFunction(()=>{
                const m=window.__BP_V5000_WORLD?.map,d=m?.getSource('bpV5041XrayFloors')?.serialize?.()?.data;
                return !!m?.getLayer('bp-v5041-xray-floors')&&Array.isArray(d?.features)&&d.features.length>0;
              },{timeout:12000});
              console.log('BRIDGEPOINT_XRAY_FLOORS_RENDERED');

              // Keep the verified building selected while exercising the measurement logic through its production-state hook.
              const measureState=await p.evaluate(()=>{
                if(typeof window.__BP_V5000_SET_MEASURE__!=='function')throw new Error('measure setter missing');
                const activated=window.__BP_V5000_SET_MEASURE__(true);
                const m=window.__BP_V5000_WORLD?.map;if(!m)throw new Error('measure map missing');
                if(typeof window.__BP_V5000_MEASURE_POINT__!=='function')throw new Error('measure point hook missing');
                const c=m.getCenter();
                const accepted=[
                  window.__BP_V5000_MEASURE_POINT__(c.lng,c.lat),
                  window.__BP_V5000_MEASURE_POINT__(c.lng+.001,c.lat+.001),
                  window.__BP_V5000_MEASURE_POINT__(c.lng+.0015,c.lat)
                ];
                return {activated,accepted,state:window.__BP_MEASURE_STATE__};
              });
              console.log('BRIDGEPOINT_MEASURE_STATE',JSON.stringify(measureState));
              if(measureState.activated!==true||measureState.accepted.some(x=>x!==true)||Number(measureState.state?.pointCount)<3||measureState.state?.hasLine!==true||measureState.state?.hasPolygon!==true||Number(measureState.state?.areaM2)<=0||Number(measureState.state?.totalDistanceM)<=0)throw new Error('MEASURE_STATE_INVALID '+JSON.stringify(measureState));
              console.log('BRIDGEPOINT_MEASURE_PASS');

              let refreshedStatus=null;
              for(let statusAttempt=1;statusAttempt<=3;statusAttempt++){
                refreshedStatus=await p.evaluate(async()=>typeof window.__BP_REFRESH_STATUS__==='function'?await window.__BP_REFRESH_STATUS__():null);
                if(Number(refreshedStatus?.canonical_properties||0)>0&&Number(refreshedStatus?.transport_rows_archived||0)>0)break;
                if(statusAttempt<3)await new Promise(r=>setTimeout(r,4000));
              }
              console.log('BRIDGEPOINT_BACKEND_STATUS_REFRESHED',JSON.stringify(refreshedStatus||{}));
              await p.waitForFunction(()=>{
                const canonical=document.getElementById('canonicalProperties')?.textContent;
                const transport=document.getElementById('transportArchive')?.textContent;
                return canonical&&canonical!=='—'&&canonical!=='0'&&transport&&transport!=='—'&&!/^0 rows/.test(transport);
              },{timeout:18000});

              const out=await p.evaluate(()=>({
                version:window.__BP_FRONTEND_VERSION,
                canvases:document.querySelectorAll('#liveMap canvas').length,
                canonical:document.getElementById('canonicalProperties')?.textContent,
                horizonLinks:[...document.querySelectorAll('a')].filter(a=>/horizon/i.test(a.href+a.textContent)).length,
                panel:!!document.getElementById('systemPanel'),
                contextTab:!!document.getElementById('bpTabContext'),
                xrayEvidence:!!document.getElementById('bXrayUnderground'),
                liveButton:!!document.getElementById('toggleLiveContext'),
                liveLayer:!!window.__BP_V5000_WORLD?.map?.getLayer('bp-v5004-weather-points'),
                placeLabels:!!window.__BP_V5000_WORLD?.map?.getLayer('gta-place-label'),
                roadLabels:!!window.__BP_V5000_WORLD?.map?.getLayer('gta-road-label-major'),
                poiLabels:!!window.__BP_V5000_WORLD?.map?.getLayer('gta-poi-label'),
                opportunityBuildings:!!window.__BP_V5000_WORLD?.map?.getLayer('gta-opportunity-buildings'),
                opportunityLabels:!!window.__BP_V5000_WORLD?.map?.getLayer('gta-opportunity-label'),
                opportunityRendered:!!window.__BP_V5000_WORLD?.map?.getLayer('gta-opportunity-buildings'),
                renderedPlaceLabels:(()=>{const m=window.__BP_V5000_WORLD?.map;if(!m)return 0;try{return m.queryRenderedFeatures({layers:['gta-place-label']}).length}catch{return 0}})(),
                semanticLabels:Number(window.__BP_SEMANTIC_STATS?.features||0),
                buildingAnchoredSemanticLabels:Number(window.__BP_SEMANTIC_STATS?.anchored||0),
                semanticCandidates:Number(window.__BP_SEMANTIC_STATS?.candidates||0),
                semanticClassified:Number(window.__BP_SEMANTIC_STATS?.classified||0),
                semanticCapabilityLabels:Number(window.__BP_SEMANTIC_LAST_GOOD__?.features||0),
                semanticCapabilityAnchored:Number(window.__BP_SEMANTIC_LAST_GOOD__?.anchored||0),
                backendCanonical:Number(window.__BP_FRONTEND_STATUS__?.canonical_properties||0),
                backendTransportArchive:Number(window.__BP_FRONTEND_STATUS__?.transport_rows_archived||0),
                xrayFloorCount:(()=>{const d=window.__BP_V5000_WORLD?.map?.getSource('bpV5041XrayFloors')?.serialize?.()?.data;return d?.features?.length||0})(),
                measureFeatureCount:Number(window.__BP_MEASURE_STATE__?.featureCount||0),
                touchZoomEnabled:!!window.__BP_V5000_WORLD?.map?.touchZoomRotate?.isEnabled?.(),
                dragPanEnabled:!!window.__BP_V5000_WORLD?.map?.dragPan?.isEnabled?.(),
                searchCollapsed:(()=>{const f=document.getElementById('publicSearchForm');return !f||getComputedStyle(f).visibility==='hidden'||getComputedStyle(f).display==='none'||Number(getComputedStyle(f).opacity)===0})(),
                liveLegend:document.getElementById('liveLegendCount')?.textContent,
                surfaceMaterial:!!document.getElementById('ctxSurfaceMaterial'),
                surfaceSource:!!document.getElementById('ctxSurfaceSource'),
                picked3D:!!document.getElementById('bPicked3D'),
                pickedTop:!!document.getElementById('bPickedTop'),
                pickedParcelArea:document.getElementById('bPickedParcelArea')?.textContent,
                pickedLayoutTruth:document.getElementById('bPickedLayoutTruth')?.textContent,
                pickedTruth:document.getElementById('bPickedTruth')?.textContent,
                pickedRoof:document.getElementById('bXrayRoof')?.textContent,
                pickedLayoutNote:document.getElementById('bPickedLayoutNote')?.textContent,
                parcelCutoutState:window.__BP_PARCEL_CUTOUT_STATE__||null,
                pickedAlignment:window.__BP_PICKED_ALIGNMENT__||null,
                selectedGeometryState:window.__BP_SELECTED_GEOMETRY_STATE__||null,
                selectedParcelLayerState:window.__BP_SELECTED_PARCEL_LAYER_STATE__||null,
                transportArchive:document.getElementById('transportArchive')?.textContent,
                transportRuntime:document.getElementById('transportRuntime')?.textContent,
                transportArchiveElement:!!document.getElementById('transportArchive'),
                transportRuntimeElement:!!document.getElementById('transportRuntime'),
                buildingPanelVisible:document.getElementById('buildingPanel')?.hidden===false,
                buildingTitle:document.getElementById('buildingTitle')?.textContent,
                buildingHeight:document.getElementById('bHeight')?.textContent,
                buildingFloors:document.getElementById('bFloors')?.textContent,
                buildingSource:document.getElementById('bSource')?.textContent,
                buildingFootprint:document.getElementById('bFootprintArea')?.textContent,
                buildingDataset:document.getElementById('bProvDataset')?.textContent,
                selectedBuildingCount:(()=>{const d=window.__BP_V5000_WORLD?.map?.getSource('bpV5000SelectedBuilding')?.serialize?.()?.data;return d?.features?.length||0})(),
                selectedParcelCount:(()=>{const d=window.__BP_V5000_WORLD?.map?.getSource('bpV5001SelectedParcel')?.serialize?.()?.data||window.__BP_SELECTED_PARCEL_SOURCE_DATA__;return d?.features?.length||0})(),
                legacySelectedCount:(()=>{const d=window.__BP_V5000_WORLD?.map?.getSource('selected')?.serialize?.()?.data;return d?.features?.length||0})(),
                buildingResolutionMethod:document.getElementById('bProvPolicy')?.textContent,
                buildingTruthBadge:document.getElementById('buildingTruthBadge')?.textContent
              }));
              if(errors.length||out.version!==5000||out.canvases!==1||out.horizonLinks!==0||!out.panel||!out.contextTab||!out.xrayEvidence||!out.liveButton||!out.liveLayer||!out.placeLabels||!out.roadLabels||!out.poiLabels||!out.opportunityBuildings||!out.opportunityLabels||!out.opportunityRendered||out.semanticCapabilityLabels<1||out.semanticCapabilityAnchored<1||out.measureFeatureCount<3||!out.touchZoomEnabled||!out.dragPanEnabled||!out.searchCollapsed||!out.liveLegend||out.liveLegend==='CHECKING'||!out.surfaceMaterial||!out.surfaceSource||!out.transportArchiveElement||!out.transportRuntimeElement||out.selectedBuildingCount!==1||out.selectedParcelCount!==1||!out.picked3D||!out.pickedTop||!out.pickedParcelArea||out.pickedParcelArea==='—'||!out.pickedTruth||!out.pickedTruth.includes('ROOF')||!out.pickedRoof||out.pickedRoof==='—'||out.pickedLayoutTruth!=='LAYOUT UNAVAILABLE'||!/will not invent|no exact vector layout/i.test(out.pickedLayoutNote||'')||out.selectedGeometryState?.source!=='backend_exact'||out.pickedAlignment?.insideParcel!==true||out.selectedParcelLayerState?.ready!==true||out.legacySelectedCount!==0)throw new Error('V5000 Android smoke failed '+JSON.stringify({out,errors}));
              console.log('BRIDGEPOINT_V5000_ANDROID_PASS',JSON.stringify(out));
            }finally{await browser.close()}
          })().catch(e=>{console.error(e.stack||e);process.exit(1)});
          NODE
