name: Cancel shared PA1 rescue
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cancel-shared-pa1-v2786.yml'
concurrency:
  group: bridgepoint-current-building-crosswalk-rescue-subparts-PA-1
  cancel-in-progress: true
jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseding redundant rescue capacity."
