name: Recover BridgePoint V442 Deploy
# Sole recovery trigger for the freshly registered audited V442 publisher identity.

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/recover-v442-deploy.yml
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  actions: write
  contents: write

concurrency:
  group: bridgepoint-v442-deploy-recovery
  cancel-in-progress: true

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check durable release receipt
        id: receipt
        shell: bash
        run: |
          set -euo pipefail
          version="$(python - <<'PY'
          import json
          from pathlib import Path
          p=Path('tracking-release-status.json')
          try:
              print(int(json.loads(p.read_text(encoding='utf-8')).get('app_release_version',0)))
          except Exception:
              print(0)
          PY
          )"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Current durable app release: V${version}"

      - name: Dispatch existing audited V442 publisher
        id: dispatch
        if: steps.receipt.outputs.version != '442'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -u
          set +e
          response="$(gh api --method POST \
            -H 'Accept: application/vnd.github+json' \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "/repos/${GITHUB_REPOSITORY}/dispatches" \
            -f event_type=bridgepoint_v442_release \
            -F 'client_payload[reason]=v442_recovery' 2>&1)"
          code=$?
          set -e
          clean="$(printf '%s' "$response" | tr '\n\r' '  ' | head -c 1800)"
          echo "dispatch_exit_code=$code" >> "$GITHUB_OUTPUT"
          echo "dispatch_message=$clean" >> "$GITHUB_OUTPUT"
          if [ "$code" -eq 0 ]; then
            echo 'dispatch_status=REQUESTED' >> "$GITHUB_OUTPUT"
            echo 'Dispatched the sole audited V442 publisher through repository_dispatch.'
          else
            echo 'dispatch_status=FAILED' >> "$GITHUB_OUTPUT"
            echo "Publisher dispatch failed with exit code $code: $clean"
          fi

      - name: No-op after V442 receipt exists
        if: steps.receipt.outputs.version == '442'
        run: echo 'V442 durable receipt already exists; recovery is idle.'

      - name: Persist recovery diagnostic
        if: always()
        env:
          RECEIPT_VERSION: ${{ steps.receipt.outputs.version }}
          DISPATCH_STATUS: ${{ steps.dispatch.outputs.dispatch_status }}
          DISPATCH_EXIT_CODE: ${{ steps.dispatch.outputs.dispatch_exit_code }}
          DISPATCH_MESSAGE: ${{ steps.dispatch.outputs.dispatch_message }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json, os
          from datetime import datetime, timezone
          receipt_version=int(os.environ.get('RECEIPT_VERSION') or 0)
          status=os.environ.get('DISPATCH_STATUS') or ('IDLE_V442' if receipt_version == 442 else 'DISPATCH_FAILED')
          exit_code=os.environ.get('DISPATCH_EXIT_CODE')
          payload={
            'checked_at': datetime.now(timezone.utc).isoformat().replace('+00:00','Z'),
            'durable_receipt_version': receipt_version,
            'recovery_status': status,
            'dispatch_exit_code': int(exit_code) if exit_code and exit_code.isdigit() else None,
            'dispatch_message': (os.environ.get('DISPATCH_MESSAGE') or '')[:1800],
            'publisher_workflow': '.github/workflows/deploy-v442-live.yml',
            'publisher_ref': 'main',
            'alternate_artifact_path_created': False
          }
          open('recovery-v442-status.json','w',encoding='utf-8').write(json.dumps(payload,indent=2)+'\n')
          print(json.dumps(payload,indent=2))
          PY
          git config user.name 'BridgePoint Release Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add recovery-v442-status.json
          git commit -m 'Record V442 recovery dispatch status [skip ci]' || exit 0
          for attempt in 1 2 3; do
            if git push origin HEAD:main; then exit 0; fi
            git fetch origin main
            git rebase origin/main || { git rebase --abort || true; exit 0; }
          done
          exit 0
