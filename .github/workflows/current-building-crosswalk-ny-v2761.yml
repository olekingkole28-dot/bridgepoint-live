name: Current Building Crosswalk NY v2761

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/current-building-crosswalk-ny-v2761.yml'

permissions:
  contents: read

concurrency:
  group: bridgepoint-current-building-crosswalk-ny-v2761
  cancel-in-progress: true

jobs:
  stop-old-ny-run:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Old NY-only crosswalk stopped; main clipped-archive crosswalk now owns NY."
