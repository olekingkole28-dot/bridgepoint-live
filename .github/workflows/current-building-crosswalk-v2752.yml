name: Current Building Crosswalk v2752

# Superseded by fixed-lane v2772. Kept as a safety gate so future script edits
# cannot relaunch the slow one-runner-per-state matcher.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: bridgepoint-current-building-crosswalk-v2752
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Superseded by current-building-crosswalk-parallel-v2772"
