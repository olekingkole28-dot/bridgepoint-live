name: BridgePoint Legacy Pages Lane Neutralizer

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/deploy-maintenance-lockdown-v2800.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: bridgepoint-live-pages
  cancel-in-progress: true

jobs:
  neutralize:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Legacy BridgePoint Pages lane neutralized; no deployment performed."
