name: Deploy BridgePoint AI Discovery Surfaces V673

on:
  push:
    branches: [main]
    paths:
      - founder/**
      - answers/**
      - press/**
      - start-now/**
      - partners/**
      - commerce/**
      - free-tools/**
      - acquisition_tracker.js
      - articles/index.html
      - articles/feed.xml
      - articles/atom.xml
      - articles/feed.json
      - articles/data.json
      - articles/datapackage.json
      - articles/dcat.ttl
      - articles/ct-opportunity-aggregates.csv
      - articles/bridgepoint-facts.json
      - articles/bridgepoint-connecticut-property-intelligence-pulse-2026-08-23.html
      - llms.txt
      - llms-full.txt
      - robots.txt
      - sitemap.xml
      - .github/workflows/deploy-ai-discovery-surfaces-v669.yml
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: bridgepoint-live-pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Overlay public acquisition and discovery surfaces onto durable live site
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin live-artifact:refs/remotes/origin/live-artifact
          rm -rf site
          mkdir -p site
          git archive --format=tar origin/live-artifact | tar -xf - -C site

          for dir in founder answers press start-now partners commerce free-tools; do
            test -d "$dir"
            rm -rf "site/$dir"
            mkdir -p "site/$dir"
            cp -a "$dir/." "site/$dir/"
          done
          cp acquisition_tracker.js site/acquisition_tracker.js
          mkdir -p site/articles
          for file in index.html feed.xml atom.xml feed.json data.json datapackage.json dcat.ttl ct-opportunity-aggregates.csv bridgepoint-facts.json bridgepoint-connecticut-property-intelligence-pulse-2026-08-23.html; do
            test -s "articles/$file"
            cp "articles/$file" "site/articles/$file"
          done
          cp llms.txt llms-full.txt robots.txt sitemap.xml site/

          # Protect customer app from public-content deployments.
          test -s site/app/main.dart.js
          grep -q 'display_name' site/app/main.dart.js
          grep -q 'Open Account & Settings for' site/app/main.dart.js
          grep -q 'Account & Settings' site/app/main.dart.js

          # Public acquisition/discovery contracts.
          test -s site/founder/index.html
          test -s site/answers/index.html
          test -s site/press/index.html
          test -s site/start-now/index.html
          test -s site/partners/index.html
          test -s site/partners/program.json
          test -s site/partners/program.yaml
          test -s site/commerce/index.html
          test -s site/commerce/openai-products.csv
          test -s site/free-tools/index.html
          test -s site/acquisition_tracker.js
          grep -q 'Kole Johnson' site/founder/index.html
          grep -q 'Founding Performance Partners' site/partners/index.html
          grep -q '30%' site/partners/program.yaml
          grep -q 'BP-HOME-MONTHLY' site/commerce/openai-products.csv
          grep -q 'is_eligible_search' site/commerce/openai-products.csv
          grep -q 'Free Property Intelligence Snapshot' site/free-tools/index.html
          grep -q 'Founding customer sprint' site/free-tools/index.html
          grep -q 'bridgepoint-public-property-snapshot-v673' site/free-tools/index.html
          grep -q 'bp_referral_v670' site/acquisition_tracker.js
          grep -q 'Connecticut Property Intelligence Pulse' site/articles/feed.xml
          grep -q 'Kole Johnson' site/articles/data.json

          if grep -R -E -i 'SUPABASE_SERVICE_ROLE_KEY[[:space:]]*=|sb_secret_|OPENAI_API_KEY[[:space:]]*=|ANTHROPIC_API_KEY[[:space:]]*=|sk-proj-|sk-ant-|BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY' site/founder site/answers site/press site/start-now site/partners site/commerce site/free-tools site/llms.txt site/llms-full.txt site/articles; then
            echo 'Privileged secret marker found in public surfaces.' >&2
            exit 1
          fi
          touch site/.nojekyll

      - name: Persist conversion-enhanced durable live artifact
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/final-live && mkdir /tmp/final-live
          cp -a site/. /tmp/final-live/
          cd /tmp/final-live
          git init
          git config user.name 'BridgePoint Release Bot'
          git config user.email 'actions@users.noreply.github.com'
          git checkout -b live-artifact
          git add -A
          git commit -m 'BridgePoint V673 free property snapshot conversion funnel'
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push --force origin live-artifact

      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - name: Publish V673
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Verify acquisition, commerce and avatar contracts
        shell: bash
        run: |
          set -euo pipefail
          for url in \
            'https://bridgepointintelligence.online/founder/' \
            'https://bridgepointintelligence.online/answers/' \
            'https://bridgepointintelligence.online/press/' \
            'https://bridgepointintelligence.online/llms.txt' \
            'https://bridgepointintelligence.online/llms-full.txt' \
            'https://bridgepointintelligence.online/start-now/' \
            'https://bridgepointintelligence.online/partners/' \
            'https://bridgepointintelligence.online/partners/program.json' \
            'https://bridgepointintelligence.online/commerce/' \
            'https://bridgepointintelligence.online/commerce/openai-products.csv' \
            'https://bridgepointintelligence.online/free-tools/' \
            'https://bridgepointintelligence.online/acquisition_tracker.js' \
            'https://bridgepointintelligence.online/articles/feed.xml' \
            'https://bridgepointintelligence.online/articles/data.json'; do
            code="$(curl -L -sS --retry 6 --retry-delay 3 -o /tmp/page -w '%{http_code}' "$url" || true)"
            echo "$url -> $code"
            test "$code" = "200"
          done
          curl -fsSL --retry 6 --retry-delay 3 'https://bridgepointintelligence.online/free-tools/' | grep -q 'Free Property Intelligence Snapshot'
          curl -fsSL --retry 6 --retry-delay 3 'https://bridgepointintelligence.online/commerce/openai-products.csv' | grep -q 'BP-HOME-MONTHLY'
          curl -fsSL --retry 6 --retry-delay 3 'https://bridgepointintelligence.online/partners/' | grep -q '30%'
          curl -fsSL --retry 6 --retry-delay 3 'https://bridgepointintelligence.online/acquisition_tracker.js' | grep -q 'bp_referral_v670'
          curl -fsSL --retry 6 --retry-delay 3 'https://bridgepointintelligence.online/app/main.dart.js' -o /tmp/main.dart.js
          grep -q 'display_name' /tmp/main.dart.js
          grep -q 'Open Account & Settings for' /tmp/main.dart.js
          grep -q 'Account & Settings' /tmp/main.dart.js
